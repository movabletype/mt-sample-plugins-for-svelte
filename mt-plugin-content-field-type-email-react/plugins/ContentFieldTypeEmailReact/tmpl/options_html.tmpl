<mt:app:ContentFieldOptionGroup
   type="email">

  <mtapp:ContentFieldOption
     id="email-min_length"
     label="<__trans phrase="Min Length">">
    <input ref="min_length" type="number" name="min_length" id="email-min_length" class="form-control w-25" min="0" value={ options.min_length || 0 }>
  </mtapp:ContentFieldOption>

  <mtapp:ContentFieldOption
     id="email-max_length"
     label="<__trans phrase="Max Length">">
    <input ref="max_length" type="number" name="max_length" id="email-max_length" class="form-control w-25" min="1" value={ options.max_length || 255 }>
  </mtapp:ContentFieldOption>

  <mtapp:ContentFieldOption
     id="email-initial_value"
     label="<__trans phrase="Initial Value">">
    <input ref="initial_value" type="text" name="initial_value" id="email-initial_value" class="form-control" value={ options.initial_value }>
  </mtapp:ContentFieldOption>

</mt:app:ContentFieldOptionGroup>

